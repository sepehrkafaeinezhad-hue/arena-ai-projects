# صدای برند من (Brand Voice)

> ⚠️ مهم‌ترین فایل کل سیستم. هرچه دقیق‌تر پرش کنید، خروجی‌ها کمتر ژنریک می‌شود.
> بعد از پر کردن، این بخش بالا را حذف کنید.

## یک خط معرفی
من `<اسم/برند>` هستم و درباره `<نیچ>` برای `<مخاطب>` محتوا می‌سازم.

## لحن من (۰ تا ۱۰)
- صمیمی ↔ رسمی: `<مثلاً ۳ از ۱۰ = خیلی صمیمی>`
- ساده ↔ تخصصی: `<مثلاً ۴>`
- شوخ ↔ جدی: `<مثلاً ۵>`
- مستقیم و رک ↔ محتاط: `<مثلاً ۲ = خیلی رک>`

## کلمات و عبارت‌های امضایی من
- `<مثلاً: «بذار رک بگم…»>`
- `<...>`

## چیزی که هرگز نمی‌گویم
- کلی‌گویی انگیزشالی بدون مثال
- ادعای بدون سند (`[NEEDS SOURCE]` بگذارید)
- `<...>`

## نمونه‌های واقعی از متن‌های قبلی من
> ۳ تا ۵ نمونه از بهترین کپشن/اسکریپت‌های قبلی‌تان را اینجا paste کنید.
> همین‌ها الگوی واقعی صدای شما هستند.

### نمونه ۱
```
<متن نمونه>
```

### نمونه ۲
```
<متن نمونه>
```

## نکات زبان
- محاوره‌ای بنویس / رسمی بنویس: `<...>`
- مخاطب رو خطاب کن با: `<«شما» / «تو»>`

## سبک CTA من
`<مثلاً: «کلمه [X] رو کامنت کن تا راهنما رو برات بفرستم»>`
